# Slack API Configuration
# Estas variáveis podem ser encontradas no painel de administração do Slack API
# https://api.slack.com/apps -> Selecione seu app -> Basic Information
SLACK_APP_ID=A00000000000

# Cliente ID da sua aplicação Slack
# https://api.slack.com/apps -> Selecione seu app -> Basic Information
SLACK_CLIENT_ID=0000000000000.0000000000000

# Token de autenticação para o Bot do Slack
# https://api.slack.com/apps -> Selecione seu app -> OAuth & Permissions -> Bot User OAuth Token
SLACK_API_TOKEN=xoxb-seu-token-aqui

# Token de usuário para enviar mensagens como você mesmo (em vez do bot)
# https://api.slack.com/apps -> Selecione seu app -> OAuth & Permissions -> User OAuth Token
# Adicione os escopos: chat:write, users:read, channels:history
SLACK_USER_TOKEN=xoxp-seu-token-aqui

# ID do canal do Slack que será monitorado
# Para obter: clique com botão direito no canal no Slack -> Copiar link
# O ID é a parte final da URL (inicia com C)
SLACK_CHANNEL_ID=C00000000000

# Configuração do Ollama (opcional, já vem com valor padrão)
# Altere apenas se sua instalação do Ollama estiver em outro endereço
OLLAMA_API_URL=http://localhost:11434

# Porta da aplicação (opcional)
PORT=3000
